# Attestation & Readiness

Continuous verification of:

- local runtime readiness (deterministic-local-evidence)
- image digest + OIDC provenance (PR #12)
- Sigstore Policy Controller enforcement
- organization-restricted trust policy

Any future container image still requires its own digest-bound provenance verification.

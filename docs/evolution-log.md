# Evolution Log

## 2026-07-31 — Major evolve (0.2.0)

New capabilities:
- `config.json` + config loader for defaults
- `npm run help` — full command list
- `npm run report` — human-readable report with decision metrics
- `npm run watch` — periodic integrated check
- Decision log now also returns metrics
- Continuous readiness reads interval from config

## Previous

- Safe local reset, strengthened self-test, info/health/snapshot, live status file, pause/resume, decision log, named actions, integrated entry point

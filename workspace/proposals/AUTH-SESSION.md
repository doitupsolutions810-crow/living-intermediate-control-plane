# Chat authorization recorded (operator)

Session authority for allowlisted agent toolkit was granted by operator chat.

Rules still in force:

- Writes only under workspace/
- Admission only via plane admit-change / unattended / CI
- /revoke ends toolkit authority on the host
